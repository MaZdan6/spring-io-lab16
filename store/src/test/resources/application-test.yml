spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: "none"

---

spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false

eureka:
  client:
    enabled: false
